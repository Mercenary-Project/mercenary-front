VITE_API_BASE_URL=http://3.38.35.241:8080
VITE_API_PROXY_TARGET=http://3.38.35.241:8080
VITE_DEV_LOGIN_ENDPOINT=/api/auth/dev-login
VITE_KAKAO_CODE_EXCHANGE_ENDPOINT=/api/auth/kakao

# Prefer the backend-managed OAuth start URL for deployed environments.
# Confirm the exact path with the backend team before the production cutover.
VITE_KAKAO_LOGIN_URL=http://3.38.35.241:8080/oauth2/authorization/kakao

# Optional fallback for a direct Kakao authorize flow.
# VITE_KAKAO_REST_API_KEY=
# VITE_KAKAO_REDIRECT_URI=https://your-frontend-domain.com/login/callback
